[tool:pytest]"""











    slow: Slow running tests    integration: Integration tests    unit: Unit testsmarkers =testpaths = testsaddopts = --tb=short --strict-markers -vpython_functions = test_*python_classes = Test*python_files = tests.py test_*.py *_tests.pyDJANGO_SETTINGS_MODULE = myproject.settingspytest configuration file
"""

import os
import django
from django.conf import settings

# Configure Django settings
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'myproject.settings')

def pytest_configure():
    """Configure pytest with Django"""
    if not settings.configured:
        django.setup()

# Test database configuration
def pytest_configure(config):
    """Setup pytest"""
    from django.conf import settings
    
    if not settings.configured:
        settings.configure(
            DEBUG=True,
            DATABASES={
                'default': {
                    'ENGINE': 'django.db.backends.sqlite3',
                    'NAME': ':memory:',
                }
            },
            INSTALLED_APPS=[
                'django.contrib.auth',
                'django.contrib.contenttypes',
            ],
        )
        django.setup()
